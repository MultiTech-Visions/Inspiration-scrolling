{
  "name": "inspiration-scrolling",
  "version": "0.1.0",
  "description": "Nightly project-inspiration feed: a calm, prepared queue that replaces late-night doomscrolling.",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "functions-framework --target=app --port=${PORT:-8080}"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.88.0",
    "@google-cloud/functions-framework": "^3.4.5",
    "mysql2": "^3.11.5"
  }
}
